"""Entrypoint template for the package.

This file is generated by copier. It exposes a main class and a
`main()` function to run the application.
"""

{% set ClassName = project_name|replace('-', ' ')|replace('_', ' ')|title|replace(' ', '') %}

class {{ ClassName }}:
	"""Main application class for {{ project_name }}."""

	def __init__(self, name: str = "{{ project_name }}") -> None:
		self.name = name

	def run(self) -> int:
		"""Run the application and return an exit code.

		Returns:
			int: exit code (0 on success).
		"""
		print(f"Running {self.name}...")
		# TODO: implement application logic here
		return 0


def main() -> int:
	"""Command-line entrypoint used as `python -m <package>`."""
	app = {{ ClassName }}()
	return app.run()


if __name__ == "__main__":
	raise SystemExit(main())

